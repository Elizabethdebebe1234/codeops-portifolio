// Ethiopian cities
const cities = ["Addis Ababa", "Adama", "Bahir Dar", "Hawassa", "Dire Dawa"];

// forEach uses a callback for each city
cities.forEach((city, index) => {
  console.log(`${index + 1}. ${city}`);
});
